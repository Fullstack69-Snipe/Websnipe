{
  "name": "pf-backend",
  "version": "1.0.0",
  "description": "Backend สำหรับระบบยืม-คืนอุปกรณ์ (Preflight) — Express + PostgreSQL (ผ่านแพ็กเกจ db)",
  "main": "src/server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "smoke": "node db/smoke-test.js"
  },
  "dependencies": {
    "cookie-parser": "^1.4.7",
    "cors": "^2.8.5",
    "db": "file:../db",
    "express": "^4.19.2",
    "multer": "^2.0.0"
  },
  "devDependencies": {
    "@tsconfig/node-lts": "^24.0.1"
  }
}
