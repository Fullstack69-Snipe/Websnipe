{
  "name": "pf-backend",
  "version": "1.0.0",
  "description": "Backend สำหรับระบบยืม-คืนอุปกรณ์ (Preflight) — Express + SQLite",
  "main": "src/server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "seed": "node db/seed.js",
    "seed:reset": "node db/seed.js --reset"
  },
  "dependencies": {
    "better-sqlite3": "^11.3.0",
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "multer": "^2.0.0"
  }
}
