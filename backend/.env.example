# พอร์ตที่ backend ฟัง — ต้องเป็น 3000 ให้ตรงกับ NGINX_PROXY=http://pf-backend:3000
PORT=3000

# ที่เก็บไฟล์ฐานข้อมูล SQLite
DB_FILE=./db/data.sqlite

# ที่เก็บรูปที่อัปโหลด
UPLOAD_DIR=./uploads

# ผู้ใช้เริ่มต้นเมื่อ request ไม่ได้ส่ง header x-user-id มา (แทน CURRENT_USER_ID)
DEFAULT_USER_ID=u1
